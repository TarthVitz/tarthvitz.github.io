/**
 * simple loop function with controller
 */

function start() {
  for (let ifloop = 1; (ifloop = somethingneedloop()); );
}

function somethingneedloop() {
  let keeploop = 0;
  /**
   * something need been looped
   */
  return keeploop;
}
